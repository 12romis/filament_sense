use Arduino framework only
no dynamic allocation unless needed
keep serial logs concise
do not hardcode Wi-Fi in source; use config header
separate business logic from hardware access
prefer non-blocking loop with millis()
explain me in chat every change
